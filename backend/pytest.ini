[pytest]
pythonpath = .
env_files =
    .env
timeout = 30
markers =
    unit: tests for isolated logic (no external services)
    integration: tests for API endpoints and DB
    e2e: tests for full user journeys

[run]
source =
    app/services
    app/routes